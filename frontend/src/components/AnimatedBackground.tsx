/**
 * AnimatedBackground
 * 
 * Creates the "deep" animated gradient background that sets the mood.
 * Uses CSS animations for performance (GPU accelerated).
 */
export function AnimatedBackground() {
  return <div className="animated-bg" aria-hidden="true" />;
}
